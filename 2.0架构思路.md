步骤编号,步骤名称与核心内容,研发动作与提供文件,提示词工程 (Prompt) 与上下文设计,每一个步骤的要求与达成标准,产出给下步的意义与目标
步骤 1,打造奇门盘面文本翻译器 (数据脱敏),操作：编写 core/qimen/translator.py。提供文件：你现有的排盘系统输出的原始局盘 JSON（包含三奇六仪、星门神、落宫状态）。,不需要大模型参与。纯用 Python 编写文本拼接逻辑，把数字符号翻译成大模型听得懂的白话盘面。,标准：传入 JSON，必须稳定输出一段无歧义、包含九宫格所有符号的 Markdown 字符串。交付文件：translator.py,为大模型大脑提供唯一的**“数字图纸”**。大模型是瞎子，这一步是它的眼睛。
步骤 2,建立双轨制中央档案馆 (Session),操作：编写 core/qimen/session_manager.py。提供文件：步骤 1 翻译好的标准盘面文本。,上下文隔离设计：在内存字典中开辟两个独立区域：static_pan_context（雷打不动）与 dynamic_history（滑动窗口裁剪）。,标准：当聊天超过 10 轮，系统必须自动剪掉第 1-4 轮的对话，但绝对不准剪掉顶部盘面文本。交付文件：session_manager.py,攻克大模型的“健忘症”。确保多轮深入追问下，大模型永远不会脱离当前盘面瞎编。
步骤 3,新模型 API 联网与提示词锁死,【关键切入点】操作：编写 core/qimen/llm_client.py。提供文件：接入新模型 API（如 DeepSeek）的密钥、Base URL，配置在 .env 中。,系统提示词 (System Prompt)：“你是一位精通传统奇门遁甲的预测专家。你接下来的所有深度解答，必须严格基于给出的【当前奇门盘面基准】进行多轮推演。铁律：绝对不准脱离盘面编造符号，不准顾左右而言他！”,标准：成功跑通第一次连网请求，支持流式传输（Stream）。返回结果必须紧扣步骤 1 喂进去的符号。交付文件：llm_client.py,软件正式接通远端智慧大脑，让人设（灵魂）与记忆通道（战衣）完成第一次合体。
步骤 4,多轮连续对话驱动脚本封装,操作：编写 core/qimen/main_agent.py。提供文件：将步骤 1、2、3 的核心模块全部 import 导入进来。,完全体 Payload 组装：每次用户提问，此脚本自动去 Session 捞出【不变盘面】+【滚动的最近 5 轮历史】，无缝缝合成 API 请求发走。,标准：在终端（Terminal）运行该脚本，能够实现丝滑的、无失忆的连续对话交互。交付文件：main_agent.py,完成了后端 MVP 的闭环。此时你已经拥有了一个可以在黑盒里完美运行的“赛博解盘专家”。
步骤 5,Harness 极限抗压与记忆断言测试,操作：编写测试脚本 harness/test_qimen_memory.py。提供文件：准备 1 份测试盘面，以及 10 个环环相扣、层层递进的追问文本（如：空亡$\rightarrow怎么解\rightarrow$何时解）。,测试轰炸命令：利用代码模拟用户疯狂追问 10 次。在第 11 次时故意问它：“我们看的是哪个盘？值符在哪？”,标准：终端运行 pytest 显示 GREEN / PASSED。大模型在第 11 次必须准确说出最初盘面的值符宫位。交付文件：test_qimen_memory.py,出厂最终质检。证明你的上下文管理工程极其强悍，新模型 API 被你死死驯服，绝不越权或健忘。
