# 🚀 奇门遁甲系统：云端部署与架构脱敏复盘档案

> **记录时间**：2026-06
> **核心战略**：GitHub 存代码防丢 + Vercel 全球极速自动发布（CI/CD 流水线）

---

## 🎯 一、 标准化极速部署流程 (完全体路线)

对于一个纯前端（HTML/CSS/JS）项目，标准的工业级上线流程如下：

### 1. 引擎装载（本地 Git 快照）
在本地终端（必须确保进入项目所在文件夹，例如 `cd 奇门`），执行初始化与打包：
```bash
git init
git add .
git commit -m "Initial commit"
```

### 2. 建立云端金库（GitHub）
1. 登录 GitHub，点击 `New repository`。
2. 填写名字（如 `qimen-system`），**什么都不要勾选**（特别是不要勾选 Add a README），直接点击 Create。
3. 拿到空仓库的链接：`https://github.com/你的名字/qimen-system.git`。

### 3. 点火发射（推送到云端）
在本地终端执行上传连击：
```bash
git remote add origin https://github.com/你的名字/qimen-system.git
git branch -M main
git push -u origin main
```

### 4. 挂载全球 CDN（Vercel）
1. 打开 [Vercel](https://vercel.com/new)。
2. 在 **Import Git Repository** 处绑定你的 GitHub 账号。
3. 找到刚上传的 `qimen-system`，点击 **`Import`**，然后直接无脑点击 **`Deploy`**。
4. 部署成功后，在手机 Safari 打开专属链接，点击底部的“分享” -> **“添加到主屏幕”**，即刻变成原生全屏 APP。

---

## 💣 二、 实战踩坑与“第一性原理”解法

在本次部署战役中，我们击溃了以下四个经典的工业级陷阱：

### 坑一：终端提示 `fatal: not a git repository`
* **表象**：Git 拒绝执行任何上传指令。
* **第一性原理**：终端的工作目录（路径）停留在外层文件夹，没有进入包含 `.git` 隐藏引擎所在的子目录。
* **解法**：输入 `cd 奇门` 进入正确的文件舱。

### 坑二：图片上传后在 GitHub 上破损裂开
* **表象**：在 README 中配置了图片，但网页上显示不出图。
* **第一性原理**：Windows 操作系统默认开启了“隐藏已知文件扩展名”。当用户手动重命名文件为 `mobile.png` 时，系统底层实际保存为了双重后缀（如 `mobile.png.jpg`）。代码严丝合缝寻址自然找不到。
* **解法**：修改 Markdown 文件的寻址逻辑，迎合实际生成的双重后缀。

### 坑三：Vercel 部署后满屏爆红 `500 INTERNAL_SERVER_ERROR (FUNCTION_INVOCATION_FAILED)`
* **表象**：纯静态网页打不开，Vercel 报函数崩溃。
* **第一性原理**：Vercel 的雷达在扫描代码时，发现了本地测试用的 `server.js` 文件。它的自动化 AI 自作聪明地以为这是一个 Node.js 后端应用，于是试图将其打包成 Serverless Function 在云端跑起来，由于环境不兼容导致直接爆缸。
* **暴力解法**：
  使用代码剔除感染源：`git rm --cached server.js`。
* **降维打击（最高权限覆盖）**：
  在项目根目录新建 `vercel.json` 强控文件，命令 Vercel 停止一切后端幻想，强行将所有路由指向静态的 `index.html`：
  ```json
  {
    "cleanUrls": true,
    "rewrites": [ { "source": "/(.*)", "destination": "/index.html" } ]
  }
  ```

### 坑四：补丁打完后 Vercel 依然执迷不悟疯狂报错
* **表象**：即使删除了 `server.js` 并加了 `vercel.json`，Vercel 面板上的 `Function Invocations` 依然在增加。
* **第一性原理**：Vercel 存在极其坑爹的“初始化锚定”机制。第一次导入被污染的代码时，云端数据库已经给该项目永久打上了“后端动态”的钢印，后续的代码修改无法洗掉这层容器级缓存。
* **绝杀战术（摧毁重建大法）**：
  在 Vercel 的 `Settings -> Advanced` 里直接 **`Delete Project`**，彻底销毁旧的污染容器。随后回到主页，重新 Import 这个已经被 Git 洗干净的仓库，系统就会瞬间醒悟分配纯静态线路。

---

## 🛡 三、 V2.0 架构升级预警：大模型 API 安全脱敏机制

当项目进入 V2.0 并接入大模型（LLM API）时，如果继续采用“纯前端”架构，**把 API Key 暴露在 JS 文件中是自杀行为**（别人按 F12 即可瞬间窃取并刷爆余额）。

为了保证只有你一个人安全使用，必须启用**黑盒潜行模式（三道防线）**：

1. **物理隔离（第一道锁）**
   去 GitHub 的 `Settings -> Danger Zone` 将仓库的可见性改为 **`Private（私有）`**。
2. **云端脱敏（第二道锁 —— 工业级正统解法）**
   放弃前端直连。在项目里新建 `api/chat.js` 后端中转函数，并将真正的 API Key 锁在 Vercel 网页控制台的 **`Environment Variables（环境变量）`** 保险箱里。前端只向自己的代理后台发请求。
3. **接头暗号（第三道锁 —— 防蹭算力）**
   在向自己代理后台发请求时，加入一段硬编码的加密校验词（如特殊的密语输入）。即便网页地址外泄，别人调用接口时暗号不对，后台将直接阻断请求。
